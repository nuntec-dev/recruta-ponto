<%= form_with(model: work_time) do |form| %>
  <% if work_time.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(work_time.errors.count, "error") %> prohibited this work_time from being saved:</h2>

      <ul>
        <% work_time.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :person_id, 'Pessoa', style: "display: block" %>
    <%= collection_select :work_time, :person_id, Person.all, :id, :name, include_blank: true %>
  </div>

  <div>
    <%= form.label :started_at, 'Iniciou em', style: "display: block" %>
    <%= form.datetime_field :started_at %>
  </div>

  <div>
    <%= form.label :ended_at, 'Terminou em', style: "display: block" %>
    <%= form.datetime_field :ended_at %>
  </div>

  <div>
    <%= form.submit 'Salvar' %>
  </div>
<% end %>
